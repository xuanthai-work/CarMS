/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.DS_Store

# biến môi trường (bí mật) — chỉ commit .env.example
.env*
!.env.example

# runtime data store (giữ seed.json; bỏ db.json thực thi + các bản backup)
/data/db*.json

/lib/generated/prisma

# tài liệu spec/plan brainstorm — giữ local, không commit
/docs/superpowers

# scratch của subagent-driven-development (ledger tạm)
/.superpowers

# snippet SQL tạm của Supabase Studio (local)
/supabase/snippets
